### 核心框架：结构化你的提示词
遵循 RTF / CO-STAR 等结构化框架
Role (角色): 赋予 AI 一个专业身份（例如：你是一名资深后端架构师）。
• Task (任务): 明确你要它做什么（例如：请重构这段 Python 代码）。
• Context (背景): 提供必要的前置信息（例如：这段代码运行在内存受限的嵌入式设备上）。
• Format (格式): 指定输出的样子（例如：Markdown 表格、JSON 格式、代码块）。
• Constraint (限制): 避雷指南（例如：不要使用第三方库，字数控制在 200 字以内）。

万能提示词模板
[角色]：你是一位 [领域] 专家。
[任务]：请帮我 [具体动作] 关于 [主题]。
[要求]：
1. [具体要求 1]
2. [具体要求 2]
[输出格式]：请以 [表格/清单/报告] 形式呈现，语言风格要 [专业/幽默/简洁]。


四大核心技巧 
① 少样本提示 
逻辑： 给 AI 几个例子，它学得更快。
② 思维链
逻辑： 强制 AI “分步思考”，能显著提升复杂逻辑（如数学、编程）的准确性。
③ 设定限制与负面提示
明确告诉 AI “不要什么”。
④ 迭代反馈
